<form method="post" action="{{ path('owner_delete', {'id': owner.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce compte vendeur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ owner.id) }}">
    <button class="btn">Supprimer</button>
</form>
